#include <stdlib.h>

#ifndef _EVENTS_H_
#define _EVENTS_H_

typedef enum
{
  SHMEV_START,
  SHMEV_ERROR,
  SHMEV_PARSER_BUFFER_EMPTY,
  SHMEV_PARSER_BUFFER_OPENED,
  SHMEV_FILE_OPENED,
  SHMEV_FILE_CLOSED,
  /* PARSING EVENTS*/
  SHMEV_PARSE_START,
  SHMEV_PARSE_RUNNING,

} SHMEVENT;

SHMEVENT* init_event();

#endif

